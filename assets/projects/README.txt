Drop project screenshots here using these exact filenames
(matching the src= paths already wired up in index.html):

  smart-shopping-agent.jpg
  hiremind.jpg
  consumer-complaint-classifier.jpg
  fashion-recommendation-system.jpg
  candidate-calling-bot.jpg
  loan-approval-prediction.jpg

Recommended: JPG or WEBP, roughly 1200x600px (2:1 ratio), under 200KB each.
If a file is missing, the card automatically falls back to the
gradient + monogram placeholder — nothing will break.
